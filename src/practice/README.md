# 练习记录

## 2026-09-08：工具片段组装（02.ts）

### 目标与结果

输入是一个固定的片段数组。同一个工具的片段可能与其他工具交错出现，需要按 index 分组，保留 id、name，拼接 arguments 字符串，解析并返回完整工具数组。

已完成主要逻辑。验证通过：类型检查、两个工具交错拼接、保留字符串中的空格、处理字符串里的右大括号，以及拒绝不完整 JSON、非法尾部和数组参数。

最后一次检查时，非法 JSON 已经会抛错，但错误文本仍是 `invalid json`；已建议补上工具 index，尚未复查这一行。

### 主要感受：JS 写法与 TS 结合后，复杂度明显增加

这次主要的负担不只是分组和拼接，而是：以前熟悉的 JS 编码习惯，为了配合类型检查，需要重新组织。除了考虑“代码运行时怎么做”，还要考虑“类型系统能不能确认数据已经准备好了”。两件事一起学习，增加了理解成本。

具体表现：

- 习惯先建空对象再逐项赋值，但 `{} as Tool` 提前声明它已经完整，与后面判断字段不存在的逻辑冲突。
- 改成 `Partial<Tool>` 后，能表示组装中的状态，却不能直接放进要求完整结果的 `Tool[]`，还需要确认字段齐全。
- Map 补上键和值的类型后，读取结果还需要考虑不存在的情况。
- JSON 能成功解析，不等于解析结果符合工具参数的对象结构；接成 unknown 后，还需要验证才能使用。

这些成本是真实的。但不必把每次报错都解决成更复杂的类型技巧：这题用局部变量收集，最后检查并一次性构造完整 Tool，就比围绕空对象不断补类型更直接。Partial 本身没有错，只是本题不必使用它。

### 这次整理出的类型习惯

- 输入 Fragment 如实允许 id、name 缺失；最终 Tool 保持必要字段必填。
- 分组使用 `Map<number, Fragment[]>`，让遍历变量自动获得类型。
- 收集中尚未获得的 id、name 用 `string | undefined` 表达；content 从空字符串开始，让 TS 推断。
- 在组装结束处集中检查必要字段，再构造完整结果。
- 解析参数先接成 unknown，确认是非 null、非数组的对象；这只验证对象形状，还没有验证具体工具要求的 path 等字段。
- 不以消除红线为目的滥用 as，也不把所有字段改成可选。遇到报错，先检查类型声明是否符合数据当前的状态。

参考：[TypeScript 可选属性](https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#optional-properties)、[类型收窄](https://www.typescriptlang.org/docs/handbook/2/narrowing.html)、[Partial](https://www.typescriptlang.org/docs/handbook/utility-types.html#partialtype)。

### 与类型无关的逻辑问题

- 后续片段缺少 id、name 时，不应覆盖之前已收集的值。
- 拼接片段不能随意 trim，否则会删除 JSON 字符串中的有效空格。
- 固定数组已经包含全部片段，应在一组遍历结束后解析一次，不用寻找右大括号来猜测 JSON 是否完整。
- 解析失败应抛错，不能只打印后继续返回旧结果。

### 后续学习节奏

本题难度目前刚好：范围小，有明确输入和输出，需要独立组织逻辑，又能直接验证结果。

保持用户先写、再按实际代码评审的方式；每次集中处理一两处问题，不在一次修改中连续引入过多类型概念。说明时同时展示“当前数据是什么”和“这一步变成什么”。验证完成即收束，不不断追加要求。

## 2026-09-08 制定的下次计划：文本消息切割

今天到此结束，下次再开始。

使用固定字符串数组模拟分块接收，只练习切出完整消息，并保留未完成尾部等待下一块。不接真实 fetch，不解析 JSON，不处理工具调用；先只支持 `\n\n` 分隔符。

输入示例：

```typescript
const chunks = [
  'data: {"text":"你',
  '好"}\n\ndata: {"text":',
  '"世界"}\n\n',
];
```

期望输出：

```typescript
[
  'data: {"text":"你好"}',
  'data: {"text":"世界"}',
]
```

核心新点只有一个：当前片段不完整时保留它，等下一块接上。开始练习时先明确完成标准，再由用户自行实现。

## 2026-09-09：文本消息切割（03.ts）

### 目标与结果

使用固定字符串数组模拟分块接收，仅以 `\n\n` 分隔消息。函数返回 `{ messages: string[]; remainder: string }`：messages 保存全部完整消息（不含分隔符），remainder 保存未完成尾部。保留原始空格及空消息，不解析 JSON、不删除 data:，不接网络或工具调用。

用户独立完成第一版：跨块拼接、尾部保留和原始空格处理正确，但每收到一块只取出一条消息。输入 `['A\n\nB\n\n']` 时，B 被留在 remainder 中。教练指出反例并提问后，用户自行增加内层循环，取出当前文本中的全部完整消息，没有由教练代写实现。

验证结果：主例子、空输入、跨块拼接且有尾部、单块多消息、分隔符跨块、原始空格、空消息，共 7 个约定例子全部通过。补上显式返回类型后的 03.ts 严格单文件类型检查也通过；不代表整个项目或真实网络流已验证。练习文件实际位于原项目 D:/Project/ReAct_Agent_CLI/src/practice/03.ts。

### 核心理解

- 数组中的每一项是一次接收的 chunk，不是一条完整消息；data: 今天只当作普通文本。
- 一个值可以被分到两个 chunk，例如“你”和“好”是同一字符串值的两段；拼接整段原始文本即可，不需要取出 text 字段。
- 外循环每次接收一个 chunk，接到 pendingText 后面。
- 内循环处理 pendingText 中的所有完整消息；pendingText 是之前留下的尾部加上新 chunk，可能跨越多个 chunk。
- 找不到分隔符时退出内循环，保留尾部等待下一块。输入结束也不能把未完成尾部当成完整消息输出。

注释建议尚未修改：将“一条消息中可能存在多个\n\n”改为“当前积累的文本中可能包含多条完整消息”。这是表述修正，不影响已通过的逻辑。

### TS 补练与反馈

用户明确反馈：今天对 TS 的训练不够。今天的主要进步是数据状态理解、边界定位和独立修正逻辑，不能把逻辑验收通过等同于 TS 训练充分。

补练集中在一个真实约束：把已定义的 SplitResult 用作 splitMessages 的显式返回类型。现已读回确认签名为 `function splitMessages(chunks: string[]): SplitResult`。

教练建议临时删除空输入分支的 remainder、临时将其改为数组，分别观察类型错误后恢复。未直接观察这两个临时实验，不记录为已验证。用户理解了目的：保证所有返回路径符合完整的数据结构约定；没有消息用 []，没有尾部用空字符串，而不是省略字段。

参考：[TypeScript 返回类型标注](https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#return-type-annotations)。相关说明已通过 Context7 核对官方文档。

后续保持小范围真实任务，但应明确安排一处有实际意义的 TS 训练点，让用户自己修改、观察错误并解释原因；不为练工具类型硬塞语法，也不一次加入一串概念。

## 下次计划（2026-09-10）：解析一条完整的文本消息

输入是一条已切完整的消息，例如 `data: {"text":"你好"}`，期望输出字符串“你好”。范围只包括去掉开头的 data:、解析 JSON、验证 text 是字符串；不接网络，不整合整个 Agent，不处理工具调用。

TS 重点：外部数据先接成 unknown，经过必要验证后才能作为确定类型使用。沿用昨天的验证经验，但把业务缩小到单条文本消息。

开始时先给明确函数输入、输出、失败处理约定及少量验收例子，再让用户独立写第一版。失败处理的具体约定留到明天开题时确定；不提前给完整实现，不自行创建或修改练习代码。
